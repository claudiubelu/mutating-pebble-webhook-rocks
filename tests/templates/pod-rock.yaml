apiVersion: v1
kind: Pod
metadata:
  name: test-rock
spec:
  containers:
  - name: test-rock
    image: claudiubelu/pebble-mutating-webhook:0.0.1
    securityContext:
      readOnlyRootFilesystem: true
